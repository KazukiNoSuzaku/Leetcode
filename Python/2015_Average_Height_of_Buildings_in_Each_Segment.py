# Author: Kaustav Ghosh
# Problem 2015: Average Height of Buildings in Each Segment (Premium)
#
# Use a sweep line: at each building start add height and count,
# at each end subtract. Track segments where count > 0 and compute
# average height, merging consecutive segments with same average.

class Solution(object):
    pass
